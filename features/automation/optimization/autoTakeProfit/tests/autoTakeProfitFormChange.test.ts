import { expect } from 'chai';

test('Test autotake profit form component', () => {

    describe('User inputs values into form', () => {

        it('Should validate the input for  x', () => {

        })

        it('Should disable button if values are incorrect', () => {

        })


        it('Should enable button if values are correct', () => {

        })
    })


})